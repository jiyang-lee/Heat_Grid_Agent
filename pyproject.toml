[project]
name = "heat-grid-agent"
version = "0.1.0"
description = "Frozen legacy workspace for HeatGrid Agent branch outputs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
