# 2026-06-26 TODO

## 확인 완료

- 현재 브랜치 `proto` 확인
- `docs/report/proto` 정본 보고서 11개와 README 확인
- full supervised label CSV `3346 rows x 9 columns` 확인
- full preprocessed CSV `3346 rows x 211 columns` 확인
- 현재 mock model chain CSV `300 rows x 25 columns` 확인
- 현재 mock priority CSV `300 rows x 9 columns` 확인
- `docs/send` 초안 48개, work order 24개, email 24개 확인
- 프로토 완료 상태와 alpha 전환 보강 항목 표 정리 문서 추가
- 표 정리 문서를 raw 계약 / 전처리 계약 / 전처리 자동화 로직 계약 / DB 스키마 / 자동화 로직 / ML 모델 / 서버 / 프론트 기준으로 재분류
- `model_chain_output` DB JSON/SQL 계약 추가 및 `priority_scores`를 007번으로 이동
- 계약 검증과 전체 테스트 재실행 완료: `uv run python -m agent.priority.validate_contracts`, `uv run pytest`

## 남은 일

1. 학습용 full chain output과 serving output 경로 분리
2. alpha DB/repository 계층 설계
3. job 상태관리와 승인 workflow 설계
4. fault event group split과 time forward validation 보강
