# 2026-06-23 TODO

## 진행중

- ML팀 회신 체크리스트 정리

## 대기

## 완료

- ML팀에 데이터 요구사항 송부
- canonical prediction schema 확정
- Agent 관련 코드 작업 위치를 `agent/`로 고정
- Agent 관련 Git 작업 기준 브랜치를 `agent`로 고정
- `raw / normalized / prediction / decision / log` DB 축 정리
- `raw_sensor_events`, `normalized_sensor_features`, `model_predictions`, `agent_decisions`, `execution_logs` 역할 정리
- `model_predictions`와 `agent_decisions` 관계 정의
- DB 스키마 초안 코드 및 검토용 노트북 추가
- 노트북 실행 시 프로젝트 루트 import 경로 보정
- `ingest_normalize_tool`, `predict_adapter_tool`, `decision_tool` 입출력 정의
- PreDist 기준 tool I/O 계약 문서 추가
- Agent 팀 handoff 문서 추가

## 메모

- 오늘의 범위는 Agent A(DB 담당) 작업만 포함한다.
- `todo/`는 날짜별로 파일을 추가해서 누적 관리한다.
- canonical prediction은 `한 건의 prediction event + optional time window` 기준으로 유지한다.
