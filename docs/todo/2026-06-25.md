# 2026-06-25 TODO

## 진행 중

- 전처리 데이터 계약 검토

## 확인 완료

- 현재 브랜치 `agent1` 확인
- `data/raw/` 원천 CSV 101개, 약 1.52GB 확인
- JSON Schema 4개 JSON 문법 확인
- SQL DDL과 JSON Schema 컬럼 수 정적 대조
- 대표 원천 CSV 헤더 확인
- 운영 입력 계약 커밋 전 정리
- 전처리 데이터 계약 `preprocessed_data_v1` 추가
- `preprocessed_windows` SQL/JSON Schema 211컬럼 대조
- numeric sensor 파생 153개, control/status 파생 33개 확인
- raw 4테이블 dataframe -> `preprocessed_windows` 생성 코드 추가
- `uv run pytest` 4개 테스트 통과
- PreDist ZIP 샘플러 `agent/preprocessing/sample_predist_zip.py` 추가
- PreDist 실제 ZIP에서 sensor_readings 300행 샘플 생성
- PreDist 샘플 end-to-end 결과 `preprocessed_windows` 10행 x 211컬럼 확인
- `agent/fixtures/preprocessing/predist_sample/raw/` raw 4테이블 fixture 생성
- `agent/fixtures/preprocessing/predist_sample/output/` 전처리 결과 fixture 생성
- 원본 ZIP 없이 raw fixture 4개로 `preprocessed_windows` 재현 테스트 추가
- `uv run pytest` 6개 테스트 통과

## 커밋 전 남은 일

1. `data/raw/` 제외 상태 확인
2. 보고서의 현재 상태/검증 결과 갱신
3. 최종 `git status --short --branch` 확인
4. 커밋 대상 파일만 선별 스테이징
5. PostgreSQL + TimescaleDB 환경에서 `005_preprocessed_windows.sql` 실행 검증
6. PreDist 전체 93개 operational CSV 일괄 처리 여부 판단
