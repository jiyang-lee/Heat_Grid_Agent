# 2026-06-24 TODO

## 한 일 요약

- 파이프라인 1단계 `ingest_normalize_tool` 구현 (Agent A 오전분).
- 정답지(`agent/fixtures/data` 골든 JSON 15건) 재현 기준으로 `raw_event` + `normalized_feature` 생성.

## 조건별 수행 내역 (무엇을 / 조건에 맞춰 어떻게)

### 절대 규칙 준수

- LLM 미호출 → 순수 결정적 변환만 수행.
- DB write 없음 → `raw_sensor_events` / `normalized_sensor_features` 용 row-ready dict 만 반환.
- 이상·정상 판정 안 함, 결측 보정 안 함 → 결측 base 센서는 보정 없이 `metadata.missing_fields` 에 기록만.
- 검증 실패 시 예외 대신 구조화 실패 dict 반환 (`success` / `error` / `data`).

### '확인 필요' 4개 → 합의된 방향대로 구현

1. 파생 피처(`temperature_gap`, `flow_to_temperature_ratio`)는 이 툴에서 생성하되 `ingest_config` 로 분리.
2. `scenario` 는 `feature_values` 에 유지(설정화). mock 전용 라벨이라 실데이터 전환 시 제거 대상으로 명시.
3. ID/created_at → 계약식 ID(`raw-{substation_id}-{ts}`) 결정적 생성 + 골든 비교에서 ID 2필드 제외,
   `created_at` 은 주입 가능(미주입 시 현재 UTC).
4. metadata 키 → 골든 우선으로 `source_row` 사용(계약문서 `row_source` 와 어긋남을 기록).

### 정답 데이터 역산 검증 (15/15)

- `temperature_gap = round(p_hc1_return_temperature - outdoor_temperature, 1)`
- `flow_to_temperature_ratio = round(p_net_meter_flow / p_hc1_return_temperature, 4)`
- `feature_values` 키 = base 3 + 파생 2 + `scenario` (`timestamp` / `case_id` 제외)
- `raw.metadata` = {case_id, scenario, source_row} / `norm.metadata` = {case_id, scenario, missing_fields}
- `observed_at == raw_payload.timestamp` 전건 일치

### 파일 구성 (역할 분리)

- `agent/tools/ingest_config.py` — 통과/유지/제외 키·파생 피처·metadata 구성 설정(왜 이 값인지 why 주석 포함).
- `agent/schemas/ingest_io.py` — `IngestInput` / `RawEvent` / `NormalizedFeature` (Pydantic v2).
- `agent/tools/ingest_normalize_tool.py` — 코어 `ingest_normalize` + `@tool` 래퍼.
- `agent/tests/test_ingest_normalize_tool.py` — 골든 비교 + 실패/결측/결정성 테스트.

### 테스트 결과

- 신규 13개 + 기존 12개 = 총 25개 통과 (회귀 0).
- 골든 deep-equal: `raw_event` 15/15, `normalized_feature` 15/15 (ID 2필드 제외).
- 분기 검증: missing timestamp / missing substation_id / invalid observed_at / 센서 결측(보정 안 함) /
  결정성(동일 입력→동일 출력) / `@tool` invoke.

### 산출물 보강

- 보고서 `docs/report/03_ingest_normalize_tool_report.md` 작성(무엇을/왜/수치/한계).
- 검토용 노트북 `agent/notebooks/02_ingest_config.ipynb`, `03_ingest_normalize_tool.ipynb` 추가 후
  `nbconvert --execute` 로 위→아래 실행 검증(골든 15/15 재현).

